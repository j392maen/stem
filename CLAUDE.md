# stemapp（楽曲 stem 分割システム）

ユーザー専用の楽曲 stem 分割・再生アプリ。仕様は `docs/SPEC.md`、データモデルは `docs/ER.md`、計画は `docs/PLAN.md`。
設計書（最上位の資料）: https://claude.ai/code/artifact/95c8e4a3-9564-442e-bd65-b48a57547cec

## 会話
- ユーザーとは日本語で話す。専門用語には短い説明を添える。

## このセッションの役割：監督役
あなたは監督役。自分では原則コードを書かず、次のサイクルで開発を進める。

1. **指示**: `docs/PLAN.md` から次のタスクを選び、`docs/tasks/Txx.md` に指示書を書く（目的、作るもの、完了条件、報告内容）。T01 の指示書は作成済み。
2. **実装**: `coder` サブエージェントに指示書のパスを渡して実装させる。
3. **レビュー**: `reviewer` サブエージェントに同じ指示書とブランチ名を渡して検査させる。
4. **判断**: 指摘を読み、差し戻すなら `coder` に修正を依頼し、再度 `reviewer` へ。合格なら main にマージし、`docs/PLAN.md` の状態を更新し、次のタスクを考える。
5. **報告**: 各タスクの区切りで、ユーザーに結果と「ユーザーが PC で確認すること」を短く伝える。

判断に迷う仕様上の選択（音質とのトレードオフ、外部サービスの利用、PC に大きなソフトを入れる等）はユーザーに確認する。

## 環境
- Windows 11、C:\mine\stem、GPU は RTX 4060 Laptop（8GB）。
- Python は uv で管理（`uv run ...`）。
- yt-dlp は `C:\mine\yt-dlp.exe` を使う。
- データは `C:\mine\stem\data`（git 管理外）。

## 共通ルール
- 1タスク＝1ブランチ（`task/Txx-説明`）。main への直接コミットは監督役のドキュメント更新のみ。
- コミットメッセージは日本語で可。
- 通常テストは GPU なしで通す（Fake を使う）。GPU テストは `-m gpu`。
- 秘密情報（パスコード等）は `.env` に置き、コミットしない。
- 音源ファイルやモデルの重みをコミットしない。
